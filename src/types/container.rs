//! Container validation — re-exports from docker.rs for backward compat
//! The real types are in docker.rs (ContainerState, ContainerInspect, etc.)

// This file kept minimal — docker.rs has the container types
